# Direct dependencies only. Pin loosely (>=) unless you have a reason to pin exactly.
# Add your own dependencies below as you need them, in alphabetical order.

pandas>=2.0
pytest>=7.0

# TODO (Partner A): do you need anything else for ingest? Add it here.
# TODO (Partner B): you will need something for the plot in demo.py — add it here.
